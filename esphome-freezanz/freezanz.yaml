esphome:
  # Hostname for the device; mac_suffix differentiates multiple units
  name: espfreezanz
  friendly_name: Freezanz
  on_boot:
    priority: 600
    then:
      - script.execute: update_status_led
      - script.execute: rtc_read_time
      - lambda: |-
          id(pump_time_remaining).publish_state(NAN);
          id(pump_progress).publish_state(NAN);

esp32:
  board: esp32dev
  framework:
    type: esp-idf
    advanced:
      minimum_chip_revision: '3.0'

# Enable logging via UART
logger:
#  level: VERBOSE
#  logs:
#    i2c.idf: WARN
#    i2c.master: WARN
#    mcp23xxx: WARN
#    sensor: WARN
#    binary_sensor: WARN
#    esp32.preferences: VERBOSE

# default HA integration, OTA updater and backup http web portal
api:
  # TODO increase the security
  # password: !secret device_password
  #encryption:
  #  key: "YOUR_ENCRYPTION_KEY"
  # 5m default delay. if no home assistant, then force reboot
  reboot_timeout: 0s

# OTA update support
ota:
  - platform: esphome
  # TODO increase the security
  # password: !secret device_password
  # change to https://esphome.io/components/ota/http_request ?

wifi:
  networks:
    - ssid: !secret wifi_1_ssid
      password: !secret wifi_1_password
    - ssid: !secret wifi_2_ssid
      password: !secret wifi_2_password
  post_connect_roaming: true
  on_connect:
    then:
      - switch.turn_on: sw_led_blue_d3
  on_disconnect:
    then:
      - switch.turn_off: sw_led_blue_d3

# Local diagnostic web portal
web_server:
  port: 80
  version: 3

# ---------------------------------------------------------------------------
# UTILITY
# ---------------------------------------------------------------------------

# Test controls
button:
  - platform: template
    name: "ESPHome Restart"
    id: btn_restart
    entity_category: "config"
    icon: "mdi:restart"
    on_press:
      then:
        - lambda: |-
            esp_ota_mark_app_valid_cancel_rollback();
            ESP_LOGI("boot", "OTA marked valid before reboot");
        - delay: 200ms
        # This is the correct way to reboot via code
        - lambda: 'App.safe_reboot();'

  - platform: template
    name: "Clear Stored States"
    id: clear_states_button
    icon: "mdi:database-remove"
    entity_category: "config"
    on_press:
      then:
        - lambda: |-
            global_preferences->reset();
            ESP_LOGI("custom", "NVM Memory Cleared! Restarting...");
        - delay: 1s
        # call the restart button, to consolidate the logic
        - button.press: btn_restart

  - platform: output
    name: "Power Switch"
    id: sw_onoff_switch
    icon: "mdi:power"
    entity_category: "diagnostic"
    output: sw_onoff
    duration: 200ms

  - platform: template
    name: "Buzzer ON sequence"
    icon: "mdi:volume-high"
    entity_category: "diagnostic"
    on_press:
      then:
        - script.execute: buzzer_on_sequence

  - platform: template
    name: "Buzzer OFF sequence"
    icon: "mdi:volume-off"
    entity_category: "diagnostic"
    on_press:
      then:
        - script.execute: buzzer_off_sequence

  - platform: template
    name: "Buzzer Alarm sequence"
    icon: "mdi:alarm-light"
    entity_category: "diagnostic"
    on_press:
      then:
        - script.execute: buzzer_start_sequence

  - platform: template
    name: "Buzzer Stop sequence"
    icon: "mdi:stop"
    entity_category: "diagnostic"
    on_press:
      then:
        - script.execute: buzzer_stop_sequence

text_sensor:
  - platform: template
    name: "OTA Partition"
    id: ota_partition
    icon: "mdi:chip"
    entity_category: "diagnostic"
    lambda: |-
      const esp_partition_t *p = esp_ota_get_running_partition();
      return std::string(p->label);
    update_interval: 60s


  - platform: version
    name: "Freezanz ESPHome Version"
    icon: mdi:information
    hide_timestamp: true

  - platform: wifi_info
    ip_address:
      name: "Freezanz IP Address"
      icon: "mdi:ip-network"
    mac_address:
      name: "Freezanz MAC Address"
      icon: "mdi:network"

# Optional
  - platform: template
    name: "Concentrate Liquid Status"
    id: concentrateliquid_stato
    icon: "mdi:bottle-tonic"
    lambda: |-
      if (id(level_s3).state) return {"OK"};
      if (id(level_s2).state) return {"OK"};
      if (id(level_s1).state) return {"Warning"};
      if (id(level_s0).state) return {"Critical"};
      return {"CRITICAL"};
    update_interval: 60s

sensor:
  - platform: wifi_signal
    name: "WiFi Signal (RSSI)"
    update_interval: 60s
    icon: "mdi:wifi"
    entity_category: "diagnostic"

  - platform: template
    name: "GPIO5 P1 Low Count"
    id: water_flow_missing_counter_sensor
    icon: "mdi:counter"
    lambda: return id(water_flow_missing_counter);
    update_interval: 10s
    accuracy_decimals: 0
    unit_of_measurement: "events"
    entity_category: "diagnostic"

  - platform: template
    name: "Nebulization Time Remaining"
    id: pump_time_remaining
    icon: "mdi:timer"
    unit_of_measurement: "s"
    accuracy_decimals: 0
    device_class: duration
    lambda: |-
      if (!id(action_running) && !id(pre_cycle_running)) return {};
      int remaining;
      int elapsed = (int)((millis() - id(pump_start_time)) / 1000);
      if (id(pre_cycle_running)) {
        remaining = id(pre_cycle_duration) - elapsed;
      } else {
        remaining = id(pump_duration_number).state - elapsed;
      }
      float result = remaining > 0 ? (float)remaining : 0.0f;
      ESP_LOGI("pump", "Time remaining: %.0fs", result);
      return result;
    update_interval: 1s

  - platform: template
    name: "Nebulization Progress"
    id: pump_progress
    icon: "mdi:progress-clock"
    unit_of_measurement: "%"
    accuracy_decimals: 0
    lambda: |-
      if (!id(action_running)) return {};
      int elapsed = (int)((millis() - id(pump_start_time)) / 1000);
      float progress = (float)elapsed / (float)id(pump_duration_number).state * 100.0f;
      return progress > 100.0f ? 100.0f : progress;
    update_interval: 1s

  # GPIO36 (SENSOR_VP) — input-only ADC, no pullup available
  - platform: adc
    pin: GPIO36
    name: "Debug ADC GPIO36 VP"
    id: debug_adc36
    icon: "mdi:bug"
    update_interval: 5s
    attenuation: 12db
    entity_category: "diagnostic"
    filters:
      - delta: 0.5

  # GPIO39 (SENSOR_VN) — input-only ADC, no pullup available
  - platform: adc
    pin: GPIO39
    name: "Debug ADC GPIO39 VN"
    id: debug_adc39
    icon: "mdi:bug"
    update_interval: 5s
    attenuation: 12db
    entity_category: "diagnostic"
    filters:
      - delta: 0.5

  # GPIO34 — input-only ADC (footprint D5, unpopulated)
  - platform: adc
    pin: GPIO34
    name: "Debug ADC GPIO34"
    id: debug_adc34
    icon: "mdi:bug"
    update_interval: 5s
    attenuation: 12db
    entity_category: "diagnostic"
    filters:
      - delta: 0.5

  # GPIO35 — input-only ADC (footprint D45, unpopulated)
  - platform: adc
    pin: GPIO35
    name: "Debug ADC GPIO35"
    id: debug_adc35
    icon: "mdi:bug"
    update_interval: 5s
    attenuation: 12db
    entity_category: "diagnostic"
    filters:
      - delta: 0.5

# Optional
  - platform: template
    name: "Concentrate Liquid Level"
    id: concentrateliquid_level
    unit_of_measurement: "%"
    accuracy_decimals: 0
    icon: "mdi:bottle-tonic"
    lambda: |-
      if (id(level_s3).state) return 100.0f;
      if (id(level_s2).state) return 75.0f;
      if (id(level_s1).state) return 50.0f;
      if (id(level_s0).state) return 25.0f;
      return 0.0f;
    update_interval: 10s


time:
  - platform: sntp
    id: sntp_time
    timezone: "Europe/Rome"
    on_time_sync:
      then:
        - script.execute: rtc_write_time

binary_sensor:
  # LED status indicators — read-only, reflect actual output state
  - platform: template
    name: "WiFi Connected"
    id: status_led_blue
    icon: "mdi:wifi"
    device_class: connectivity
    lambda: return id(sw_led_blue_d3).state;

  - platform: template
    name: "Power Led"
    id: status_led_green_d7
    icon: "mdi:power"
    device_class: running
    lambda: return id(sw_led_green_d7).state;

  - platform: template
    name: "Nebulization"
    id: status_led_green_d6
    icon: "mdi:spray"
    device_class: running
    lambda: return id(sw_led_green_d6).state;

  # SW2 — ON/OFF button (GPIO33, active LOW, internal pull-up)
  - platform: gpio
    pin:
      number: GPIO33
      mode:
        input: true
        pullup: true
      inverted: true
    name: "Button ON⁄OFF"
    entity_category: ""
    icon: "mdi:power"
    id: btn_onoff
    filters:
      - delayed_on: 20ms
      - delayed_off: 20ms
    on_click:
      # 0-3 sec: power on/off
      - min_length: 0ms
        max_length: 3000ms
        then:
          - lambda: |-
              if (id(sw_device_onoff).state) {
                id(sw_device_onoff).turn_off();
              } else {
                id(sw_device_onoff).turn_on();
              }
      # >3s restart
      - min_length: 3000ms
        max_length: 10000ms
        then:
          - button.press: btn_restart
  # SW3 — START/STOP button (GPIO32, active LOW, internal pull-up)
  - platform: gpio
    pin:
      number: GPIO32
      mode:
        input: true
        pullup: true
      inverted: true
    name: "Button Start⁄Stop"
    id: btn_startstop
    icon: "mdi:play-pause"
    entity_category: "diagnostic"
    filters:
      - delayed_on: 20ms
      - delayed_off: 20ms
    on_press:
      then:
        - lambda: |-
            if (!id(sw_device_onoff).state) {
              ESP_LOGW("pump", "Device is OFF, blocking pump activation");
              return;
            }
            if (id(action_running)) {
              ESP_LOGI("pump", "Pump running — stopping");
              id(pump_start).stop();
              id(pump_stop).stop();
              id(pump_force_stop).execute();
              return;
            }
            id(pump_start).execute();

  - platform: gpio
    pin:
      number: GPIO2
      mode:
        input: true
        pullup: true
    name: "Debug GPIO2 RTC SQW state"
    id: debug_gpio2
    icon: "mdi:bug"
    entity_category: "diagnostic"

  # GPIO4 — unused, safe to probe
  - platform: gpio
    pin:
      number: GPIO4
      mode:
        input: true
        pullup: true
    name: "Debug GPIO4"
    id: debug_gpio4
    icon: "mdi:bug"
    entity_category: "diagnostic"

  # GPIO5 — P1 connector external I/O (boot strapping: must be HIGH at boot)
  - platform: gpio
    pin:
      number: GPIO5
      mode:
        input: true
        pullup: false
        pulldown: false
      inverted: false
    name: "Water Pressure Raw"
    id: sensor_water_flow_raw
    entity_category: "diagnostic"
    icon: "mdi:bug"

  - platform: template
    name: "Water Pressure"
    device_class: running
    id: sensor_water_flow
    icon: "mdi:water-pump"
    lambda: |-
      return id(sensor_water_flow_raw).state;
    filters:
      - delayed_on: 500ms
      - delayed_off: 10ms
    on_press:
      then:
        - lambda: |-
            ESP_LOGI("water", "Water pressure OK — count of missed water events: %d", id(water_flow_missing_counter));
        - script.execute: water_pressure_watchdog 
        - script.execute: update_status_led
    on_release:
      then:
        - lambda: |-
            id(water_flow_missing_counter)++;
            ESP_LOGI("water", "Water pressure lost — total missing water events: %d", id(water_flow_missing_counter));
        - script.execute: water_pressure_watchdog 
        - script.execute: update_status_led

  # GPIO14 — undocumented, verify
  - platform: gpio
    pin:
      number: GPIO14
      mode:
        input: true
        pullup: true
    name: "Debug GPIO14"
    id: debug_gpio14
    icon: "mdi:bug"
    entity_category: "diagnostic"

  # GPIO16 — unused
  - platform: gpio
    pin:
      number: GPIO16
      mode:
        input: true
        pullup: true
    name: "Debug GPIO16"
    id: debug_gpio16
    icon: "mdi:bug"
    entity_category: "diagnostic"

  # GPIO17 — unused
  - platform: gpio
    pin:
      number: GPIO17
      mode:
        input: true
        pullup: true
    name: "Debug GPIO17"
    id: debug_gpio17
    icon: "mdi:bug"
    entity_category: "diagnostic"

# Optional
  # Level sensors — XKC-Y25-NPN, NPN open-collector, pull-up interno MCP (~100kΩ)
  # NPN: LOW = liquid detected, HIGH = dry → inverted: true
  - platform: gpio
    name: "Concentrate Level S0 (min)"
    id: level_s0
    icon: "mdi:gauge-empty"
    entity_category: "diagnostic"
    pin:
      mcp23xxx: mcp23017_hub
      number: 0
      mode:
        input: true
        pullup: false
      inverted: true
    filters:
      #- delayed_on: 500ms
      #- delayed_off: 500ms
      - delayed_on: 10ms
      - delayed_off: 10ms
    on_state:
      then:
        - lambda: |-
            ESP_LOGW("level", "S0 state changed: %d", id(level_s0).state);
        - lambda: |-
            // If any sensor sees liquid, clear the orange warning
            if (id(level_s0).state || id(level_s1).state ||
                id(level_s2).state || id(level_s3).state) {
              id(out_led_red_d7).turn_off();
            } else {
              id(out_led_red_d7).turn_on();  // all dry → orange
            }
        - lambda: |-
            ESP_LOGW("level", "S0 state changed: %d", id(level_s0).state);

  - platform: gpio
    name: "Concentrate Level S1"
    id: level_s1
    icon: "mdi:gauge-low"
    entity_category: "diagnostic"
    pin:
      mcp23xxx: mcp23017_hub
      number: 1
      mode:
        input: true
        pullup: false
      inverted: true
    filters:
      - delayed_on: 500ms
      - delayed_off: 500ms

  - platform: gpio
    name: "Concentrate Level S2"
    id: level_s2
    icon: "mdi:gauge"
    entity_category: "diagnostic"
    pin:
      mcp23xxx: mcp23017_hub
      number: 2
      mode:
        input: true
        pullup: false
      inverted: true
    filters:
      - delayed_on: 500ms
      - delayed_off: 500ms

  - platform: gpio
    name: "Concentrate Level S3 (Max)"
    id: level_s3
    icon: "mdi:gauge-full"
    entity_category: "diagnostic"
    pin:
      mcp23xxx: mcp23017_hub
      number: 3
      mode:
        input: true
        pullup: false
      inverted: true
    filters:
      - delayed_on: 500ms
      - delayed_off: 500ms

output:
  # D6 — Green LED (activity in progress - e.g. nebulization)
  - platform: gpio
    pin: GPIO18
    id: out_led_green_d6


  # D3 — Blue LED (WiFi)
  - platform: gpio
    pin: GPIO19
    id: out_led_blue_d3


  # D7 — Red LED (alarm)
  - platform: gpio
    pin: GPIO25
    id: out_led_red_d7


  # D7 — Green LED (readiness)
  - platform: gpio
    pin: GPIO23
    id: out_led_green_d7


  # Buzzer on GPIO26 — frequency adjustable via number entity
  - platform: ledc
    pin: GPIO26
    id: out_buzzer
    frequency: 2500Hz

  # GPIO13 — virtual ON/OFF driver via Q4 transistor (hypothesis from RE docs)
  - platform: gpio
    pin: GPIO13
    id: sw_onoff

  # GPIO27 — pump relay driver R24->R23->Q7->X20->P-
  - platform: gpio
    pin: GPIO27
    id: sw_pump_on

select:
  - platform: template
    name: "Buzzer Frequency"
    id: buzzer_freq
    icon: "mdi:sine-wave"
    entity_category: "diagnostic"
    options:
      - "1500"
      - "2500"
      - "4000"
      - "5000"
    initial_option: "4000"
    optimistic: true
    on_value:
      then:
        - lambda: |-
            // Update LEDC frequency from dropdown selection
            float freq = atof(x.c_str());
            id(out_buzzer).update_frequency(freq);
  - platform: template
    name: "Buzzer Volume"
    id: buzzer_volume_select
    icon: "mdi:volume-medium"
    entity_category: "diagnostic"
    options:
      - "low"
      - "medium"
      - "high"
    initial_option: "medium"
    optimistic: true
    on_value:
      then:
        - lambda: |-
            if (x == "low") { id(buzzer_duty) = 0.1; }
            if (x == "medium") { id(buzzer_duty) = 0.3; }
            if (x == "high")  { id(buzzer_duty) = 0.5; }
            ESP_LOGI("buzzer", "Volume set to %s (duty=%.1f)", x.c_str(), id(buzzer_duty));

switch:
  - platform: template
    name: "Device ON⁄OFF"
    id: sw_device_onoff
    icon: "mdi:power"
    restore_mode: RESTORE_DEFAULT_OFF
    optimistic: true
    turn_on_action:
      - lambda: |-
          id(buzzer_on_sequence).execute();
          ESP_LOGI("onoff", "Device ON");
    turn_off_action:
      - lambda: |-
          id(buzzer_off_sequence).execute();
          ESP_LOGI("onoff", "Device OFF");
    # executed at every change, also turn_of and turn_off
    on_state:
      - script.execute: update_status_led

  - platform: template
    name: "Nebulization Start⁄Stop"
    id: sw_nebulization
    icon: "mdi:spray"
    restore_mode: ALWAYS_OFF
    lambda: return id(action_running) || id(pre_cycle_running);
    turn_on_action:
      - lambda: |-
          if (!id(sw_device_onoff).state) {
            ESP_LOGW("pump", "Device is OFF, blocking pump activation");
            return;
          }
          ESP_LOGI("pump", "Started nebulization pre-cycle - audible warning");
          id(pump_start).execute();
    turn_off_action:
      - lambda: |-
          id(pump_start).stop();
          id(pump_stop).stop();
          id(pump_force_stop).execute();

  - platform: template
    name: "Buzzer Enable"
    id: buzzer_enable_switch
    icon: "mdi:bell"
    restore_mode: RESTORE_DEFAULT_ON
    turn_on_action:
      - lambda: |-
          ESP_LOGI("buzzer", "Buzzer enabled");
    turn_off_action:
      - lambda: |-
          ESP_LOGI("buzzer", "Buzzer disabled");

  - platform: template
    name: "Buzzer Test"
    id: buzzer_test
    icon: "mdi:bell-ring"
    entity_category: "diagnostic"
    optimistic: true
    on_turn_on:
      - output.set_level:
          id: out_buzzer
          level: 50%   # 50% duty cycle = turn on - can't regulate it
    on_turn_off:
      - output.set_level:
          id: out_buzzer
          level: 0%

  - platform: output
    name: "Nebulization LED"
    id: sw_led_green_d6
    icon: "mdi:spray"
    output: out_led_green_d6
    entity_category: "diagnostic"

  - platform: output
    name: "WiFi Connected"
    id: sw_led_blue_d3
    icon: "mdi:wifi"
    output: out_led_blue_d3
    entity_category: "diagnostic"

  - platform: output
    name: "Water missing"
    id: sw_led_red_d7
    icon: "mdi:water-alert"
    entity_category: "diagnostic"
    output: out_led_red_d7
    on_turn_on:
      then:
        - script.execute: update_status_led

  - platform: output
    name: "Power LED"
    id: sw_led_green_d7
    icon: "mdi:led-on"
    entity_category: "diagnostic"
    output: out_led_green_d7
    on_turn_on:
      then:
        - switch.turn_off: sw_led_red_d7

  - platform: output
    name: "Debug Pump Relay"
    id: sw_pump_on_switch
    output: sw_pump_on
    entity_category: "diagnostic"
    icon: "mdi:pump"


i2c:
  sda: GPIO21
  scl: GPIO22
  scan: true
  #frequency: 100kHz
  #frequency: 10kHz
  frequency: 5kHz
  id: bus_i2c

# --- RTC helpers -----------------------------------------------------------
globals:
  - id: rtc_address
    type: uint8_t
    restore_value: no
    initial_value: '0x6F'
  - id: action_running
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: pre_cycle_running
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: pre_cycle_duration
    type: int
    restore_value: no
    initial_value: '10'
  - id: pump_start_time
    type: uint32_t
    restore_value: no
    initial_value: '0'
  - id: buzzer_duty
    type: float
    restore_value: yes
    initial_value: '0.3'
  - id: water_flow_missing_counter
    type: int
    restore_value: yes
    initial_value: '0'

number:
  - platform: template
    name: "Nebulization time (seconds)"
    id: pump_duration_number
    icon: "mdi:timer-edit"
    min_value: 10
    max_value: 120
    step: 5
    initial_value: 90
    unit_of_measurement: "s"
    mode: slider
    restore_value: true
    optimistic: true
    #lambda: return id(pump_duration);
    set_action:
      - lambda: |-
          //id(pump_duration) = (int)x;
          //ESP_LOGI("pump", "Duration set to %ds", id(pump_duration));
          ESP_LOGI("pump", "Duration set to %ds", (int)x);

script:
  - id: rtc_write_time
    then:
      - lambda: |-
          auto dec2bcd = [](uint8_t v) -> uint8_t {
            return ((v / 10) << 4) | (v % 10);
          };

          ESP_LOGI("rtc_write", "rtc_write_time invoked");

          // Get current system time via POSIX
          time_t now_epoch = ::time(nullptr);
          if (now_epoch < 1000000000) {
            ESP_LOGW("rtc_write", "System time not valid (epoch=%lld), skipping RTC write", (long long)now_epoch);
            return;
          }

          struct tm *t = localtime(&now_epoch);
          ESP_LOGI("rtc_write", "Writing to RTC: %04d-%02d-%02d %02d:%02d:%02d",
            t->tm_year + 1900, t->tm_mon + 1, t->tm_mday,
            t->tm_hour, t->tm_min, t->tm_sec);

          uint8_t buf[8];
          buf[0] = 0x00;                                    // starting register
          buf[1] = dec2bcd(t->tm_sec)   | 0x80;            // seconds + ST=1 (oscillator start)
          buf[2] = dec2bcd(t->tm_min);
          buf[3] = dec2bcd(t->tm_hour);                    // 24h mode, bit6=0
          buf[4] = t->tm_wday == 0 ? 7 : t->tm_wday;      // tm_wday: 0=Sun → MCP: 1-7
          buf[5] = dec2bcd(t->tm_mday);
          buf[6] = dec2bcd(t->tm_mon + 1);
          buf[7] = dec2bcd(t->tm_year - 100);              // years since 2000

          i2c::ErrorCode err = id(bus_i2c).write(id(rtc_address), buf, 8, true);
          if (err == i2c::ERROR_OK) {
            ESP_LOGI("rtc_write", "RTC write OK");
          } else {
            ESP_LOGE("rtc_write", "MCP7940M write failed, error: %d", err);
          }

  - id: rtc_read_time
    then:
      - lambda: |-
          auto bcd2dec = [](uint8_t v) -> uint8_t {
            return ((v >> 4) * 10) + (v & 0x0F);
          };

          ESP_LOGI("rtc_read", "rtc_read_time invoked");

          uint8_t reg = 0x00;
          uint8_t data[7] = {0};

          id(bus_i2c).write(id(rtc_address), &reg, 1, false);
          i2c::ErrorCode err = id(bus_i2c).read(id(rtc_address), data, 7);

          if (err != i2c::ERROR_OK) {
            ESP_LOGE("rtc_read", "MCP7940M read failed, error: %d", err);
            return;
          }

          // Check ST bit (bit7 of seconds register) — oscillator must be running
          if (!(data[0] & 0x80)) {
            ESP_LOGW("rtc_read", "MCP7940M oscillator not running, skipping time restore");
            return;
          }

          struct tm t = {};
          t.tm_sec   = bcd2dec(data[0] & 0x7F);
          t.tm_min   = bcd2dec(data[1] & 0x7F);
          t.tm_hour  = bcd2dec(data[2] & 0x3F);
          t.tm_mday  = bcd2dec(data[4] & 0x3F);
          t.tm_mon   = bcd2dec(data[5] & 0x1F) - 1;  // tm_mon is 0-based
          t.tm_year  = bcd2dec(data[6]) + 100;        // years since 1900
          t.tm_isdst = -1;

          ESP_LOGI("rtc_read", "RTC raw read: %04d-%02d-%02d %02d:%02d:%02d",
            t.tm_year + 1900, t.tm_mon + 1, t.tm_mday,
            t.tm_hour, t.tm_min, t.tm_sec);

          time_t epoch = mktime(&t);
          if (epoch == -1) {
            ESP_LOGW("rtc_read", "mktime failed, invalid RTC data");
            return;
          }

          // Set system clock via POSIX
          struct timeval tv = { .tv_sec = epoch, .tv_usec = 0 };
          settimeofday(&tv, nullptr);

          ESP_LOGI("rtc_read", "RTC→system time set: epoch=%lld", (long long)epoch);

  - id: rtc_debug_poll
    then:
      - script.execute: rtc_read_time
      - lambda: |-
          // Log system time after RTC read for comparison
          time_t sys_epoch = ::time(nullptr);
          struct tm *s = localtime(&sys_epoch);
          ESP_LOGI("rtc_poll", "System : %04d-%02d-%02d %02d:%02d:%02d",
            s->tm_year + 1900, s->tm_mon + 1, s->tm_mday,
            s->tm_hour, s->tm_min, s->tm_sec);

  - id: water_pressure_watchdog
    mode: restart
    then:
      - while:
          condition:
            lambda: return id(sw_pump_on_switch).state;
          then:
            - if:
                condition:
                  lambda: return !id(sensor_water_flow).state;
                then:
                  - delay: 5s
                  - if:
                      condition:
                        lambda: return !id(sensor_water_flow).state && id(sw_pump_on_switch).state;
                      then:
                        - lambda: |-
                            ESP_LOGW("water", "No water for 5s — stopping pump");
                            id(pump_start).stop();
                            id(pump_stop).stop();
                            id(pump_force_stop).execute();
            - delay: 1s

  - id: pump_start
    then:
      - lambda: |-
          // exit if no mosquito liquid is found
          if (!id(level_s0).state && !id(level_s1).state &&
              !id(level_s2).state && !id(level_s3).state) {
            ESP_LOGW("pump", "Empty tank — pump blocked");
            id(pump_start).stop();
            return;
          }


      # ensure that the led turn orange
      - script.execute: update_status_led

      # led powered on by buzzer_start_sequence
      - script.execute: buzzer_start_sequence

      - wait_until:
          condition:
            lambda: return !id(buzzer_start_sequence).is_running();

      - lambda: |-
          if (!id(sw_device_onoff).state) {
            ESP_LOGW("pump", "Device turned OFF during start sequence, aborting");
            id(pump_start).stop();
            return;
          }

      - lambda: |-
          ESP_LOGI("pump", "Pump ON — %ds cycle", (int)id(pump_duration_number).state);
          // reset to 0 the timer - in case of time drift
          id(pump_start_time) = millis();
          id(action_running) = true;
          id(sw_pump_on_switch).turn_on();

      - script.execute: update_status_led

      - delay: !lambda return id(pump_duration_number).state * 1000;  // ms

      - script.execute: pump_stop

  - id: pump_stop
    then:
      - output.set_level:
          id: out_buzzer
          level: 0%
      - script.execute: buzzer_stop_sequence
      - lambda: |-
          ESP_LOGI("pump", "Pump OFF — cycle complete");
          id(action_running) = false;
          id(sw_pump_on_switch).turn_off();
          id(pump_time_remaining).publish_state(NAN);
          id(pump_progress).publish_state(NAN);
      - output.turn_off: out_led_green_d6
      - script.execute: update_status_led
      - script.execute: buzzer_stop_sequence


  - id: pump_force_stop
    then:
      - output.set_level:
          id: out_buzzer
          level: 0%
      - lambda: |-
          ESP_LOGI("pump", "Pump force stopped");
          id(pre_cycle_running) = false;
          id(action_running) = false;
          id(sw_pump_on_switch).turn_off();
          id(pump_time_remaining).publish_state(NAN);
          id(pump_progress).publish_state(NAN);
      - script.execute: update_status_led
      - script.execute: buzzer_stop_sequence

  - id: buzzer_on_sequence
    then:
      - lambda: |-
          if (!id(buzzer_enable_switch).state) {
            ESP_LOGI("buzzer", "Buzzer disabled, skipping");
            id(buzzer_on_sequence).stop();
            return;
          }
      # Three short rising-pitch beeps — power on confirmation
      - output.ledc.set_frequency:
          id: out_buzzer
          frequency: 2200Hz
      - output.set_level:
          id: out_buzzer
          level: 50%
      - delay: 120ms
      - output.set_level:
          id: out_buzzer
          level: 0%
      - delay: 40ms

      - output.ledc.set_frequency:
          id: out_buzzer
          frequency: 2400Hz
      - output.set_level:
          id: out_buzzer
          level: 50%
      - delay: 180ms
      - output.set_level:
          id: out_buzzer
          level: 0%
      - delay: 60ms

      - output.ledc.set_frequency:
          id: out_buzzer
          frequency: 2600Hz
      - output.set_level:
          id: out_buzzer
          level: 50%
      - delay: 160ms
      - output.set_level:
          id: out_buzzer
          level: 0%

  - id: buzzer_off_sequence
    then:
      - lambda: |-
          if (!id(buzzer_enable_switch).state) {
            ESP_LOGI("buzzer", "Buzzer disabled, skipping");
            id(buzzer_on_sequence).stop();
            return;
          }
      - output.ledc.set_frequency:
          id: out_buzzer
          frequency: 2500Hz
      - output.set_level:
          id: out_buzzer
          level: 50%
      - delay: 100ms
      - output.set_level:
          id: out_buzzer
          level: 0%

  - id: buzzer_stop_sequence
    then:
      - lambda: |-
          id(buzzer_on_sequence).stop(); // why?
          if (!id(buzzer_enable_switch).state) {
            ESP_LOGI("buzzer", "Buzzer disabled, skipping");
            return;
          }
      - output.ledc.set_frequency:
          id: out_buzzer
          frequency: 2200Hz
      - output.set_level:
          id: out_buzzer
          #level: 50%
          level: !lambda return id(buzzer_duty);
      - delay: 500ms
      - output.set_level:
          id: out_buzzer
          level: 0%

  - id: buzzer_beep
    parameters:
      frequency: float
      duration_ms: int
      pause_ms: int
    then:
      - output.turn_on: out_led_green_d6
      - if:
          condition:
            lambda: return id(buzzer_enable_switch).state;
          then:
            - output.ledc.set_frequency:
                id: out_buzzer
                frequency: !lambda return frequency;
            - output.set_level:
                id: out_buzzer
                level: 50%
      - delay: !lambda return duration_ms;
      - output.turn_off: out_led_green_d6
      - output.set_level:
          id: out_buzzer
          level: 0%
      - delay: !lambda return pause_ms;

  - id: buzzer_start_sequence
    then:
      - lambda: |-
          ESP_LOGI("buzzer", "Started nebulization pre-cycle - audible warning");
          // start with a negative number to show the countdown
          id(pump_start_time) = millis();
          id(pre_cycle_running) = true;

      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 1030
          pause_ms: 940
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 810
          pause_ms: 790
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 660
          pause_ms: 640
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 500
          pause_ms: 460
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 330
          pause_ms: 280
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 310
          pause_ms: 230
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 280
          pause_ms: 200
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 210
          pause_ms: 190
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 160
          pause_ms: 140
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 140
          pause_ms: 90
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 110
          pause_ms: 60
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 80
          pause_ms: 30
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      # Two final long beeps
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 450
          pause_ms: 10
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();
      - script.execute:
          id: buzzer_beep
          frequency: 2600.0
          duration_ms: 1130
          pause_ms: 0
      - wait_until:
          condition:
            lambda: return !id(buzzer_beep).is_running();

      - lambda: |-
          ESP_LOGI("buzzer", "Completed nebulization pre-cycle - audible warning");
          // start with a negative number to show the countdown
          id(pre_cycle_running) = false;

  - id: update_status_led
    then:
      - lambda: |-
          if (!id(sw_device_onoff).state) {
            id(out_led_green_d7).turn_off();
            id(out_led_red_d7).turn_off();
            return;
          }
          if (!id(level_s0).state && !id(level_s1).state &&
              !id(level_s2).state && !id(level_s3).state) {
            id(out_led_green_d7).turn_on();
            id(out_led_red_d7).turn_on();
            ESP_LOGW("led", "Empty Tank -> LED orange");
            return;
          }
          if (!id(sensor_water_flow).state) {
            id(out_led_green_d7).turn_off();
            id(out_led_red_d7).turn_on();
            ESP_LOGW("led", "Water flow missing -> LED red");
            return;
          }
          id(out_led_green_d7).turn_on();
          id(out_led_red_d7).turn_off();
          ESP_LOGI("led", "All OK -> LED green");

interval:
  - interval: 60s
    then:
      - script.execute: rtc_debug_poll
  - interval: 5s
    then:
      - lambda: |-
          ESP_LOGW("level", "S0=%d S1=%d S2=%d S3=%d",
            id(level_s0).state, id(level_s1).state,
            id(level_s2).state, id(level_s3).state);

      - lambda: |-
          uint8_t reg;
          uint8_t valA = 0;
          uint8_t valB = 0;
          // Read port A
          reg = 0x12;
          id(bus_i2c).write(0x20, &reg, 1, false);
          id(bus_i2c).read(0x20, &valA, 1);
          // Read port B
          reg = 0x13;
          id(bus_i2c).write(0x20, &reg, 1, false);
          id(bus_i2c).read(0x20, &valB, 1);
          ESP_LOGW("mcp", "A0=%d A1=%d A2=%d A3=%d A4=%d A5=%d A6=%d A7=%d | B0=%d B1=%d B2=%d B3=%d B4=%d B5=%d B6=%d B7=%d",
            (valA>>0)&1, (valA>>1)&1, (valA>>2)&1, (valA>>3)&1,
            (valA>>4)&1, (valA>>5)&1, (valA>>6)&1, (valA>>7)&1,
            (valB>>0)&1, (valB>>1)&1, (valB>>2)&1, (valB>>3)&1,
            (valB>>4)&1, (valB>>5)&1, (valB>>6)&1, (valB>>7)&1);

      - lambda: |-
          uint8_t reg = 0x12;  // GPIO register port A
          uint8_t val = 0;
          id(bus_i2c).write(0x20, &reg, 1, false);
          id(bus_i2c).read(0x20, &val, 1);
          ESP_LOGW("mcp", "GPIO port A raw = 0x%02X", val);

      - lambda: |-
          uint8_t reg = 0x13;  // GPIO register port B
          uint8_t val = 0;
          id(bus_i2c).write(0x20, &reg, 1, false);
          id(bus_i2c).read(0x20, &val, 1);
          ESP_LOGW("mcp", "GPIO port B raw = 0x%02X", val);

      - lambda: |-
          uint8_t reg = 0x00;  // IODIRA register
          uint8_t val = 0;
          id(bus_i2c).write(0x20, &reg, 1, false);
          id(bus_i2c).read(0x20, &val, 1);
          ESP_LOGW("mcp", "IODIRA = 0x%02X (0xFF = all input)", val);
          
          reg = 0x12;  // GPIOA register
          id(bus_i2c).write(0x20, &reg, 1, false);
          id(bus_i2c).read(0x20, &val, 1);
          ESP_LOGW("mcp", "GPIOA raw = 0x%02X", val);

      - lambda: |-
          uint8_t reg;
          uint8_t val;
          // IODIRB
          reg = 0x01;
          id(bus_i2c).write(0x20, &reg, 1, false);
          id(bus_i2c).read(0x20, &val, 1);
          ESP_LOGW("mcp", "IODIRB = 0x%02X (0xFF = all input)", val);
          // GPIOB
          reg = 0x13;
          id(bus_i2c).write(0x20, &reg, 1, false);
          id(bus_i2c).read(0x20, &val, 1);
          ESP_LOGW("mcp", "GPIOB raw = 0x%02X", val);

# addon
mcp23017:
  - id: mcp23017_hub
    address: 0x20
